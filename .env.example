# ── 必填：Gateway 連線 ────────────────────────────────────────────────────────
# OpenClaw Gateway WebSocket 位址
GATEWAY_WS_URL=ws://127.0.0.1:18789

# WS Origin header（wss:// 連線時可能需要，Gateway 會校驗 Origin）
# 留空或不設 = 使用 ws 程式庫預設行為
# GATEWAY_WS_ORIGIN=https://your-gateway-host:8443

# ── 環境設定 ──────────────────────────────────────────────────────────────────
# TLS 驗證（false = 接受自簽憑證，僅限開發環境）
TLS_VERIFY=false

# CORS 允許來源（多個以逗號分隔）
CORS_ORIGINS=http://localhost:5174

# ── 客戶端識別（Gateway connect 握手用） ──────────────────────────────────────
# 客戶端 ID（需為 Gateway 白名單允許的值，預設 openclaw-control-ui）
# GATEWAY_CLIENT_ID=openclaw-control-ui
# 客戶端實例 ID（區分同類型不同部署）
# GATEWAY_CLIENT_INSTANCE_ID=openclaw-webchat-client
# 客戶端版本號
# GATEWAY_CLIENT_VERSION=0.1.0

# ── 進階設定（皆有合理預設值，通常不需修改） ──────────────────────────────────
# API_PORT=3000                        # AP 端監聽 Port
# GATEWAY_CONNECT_TIMEOUT_MS=5000      # WS 握手逾時（毫秒）
# GATEWAY_REQUEST_TIMEOUT_MS=30000     # RPC 單次請求逾時（毫秒）
# GATEWAY_HEARTBEAT_INTERVAL_MS=30000  # 心跳間隔（毫秒）；0=停用
# GATEWAY_RECONNECT_MAX_RETRIES=3      # 斷線重連次數；0=不重連
# GATEWAY_RECONNECT_DELAY_MS=1000      # 重連等待時間（毫秒，線性增長）
# STREAMING_ENABLED=true               # per-request SSE 串流開關
